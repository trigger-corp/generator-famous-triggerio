language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-triggerio' ]; then cd .. && eval "mv $currentfolder generator-triggerio" && cd generator-triggerio; fi

